import { LockScreen } from './LockScreen'

export const metadata = {
  title: 'Em manutenção | Dona B',
  robots: { index: false, follow: false },
}

export default function ManutencaoPage() {
  return <LockScreen />
}
